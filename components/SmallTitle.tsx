import React from 'react'

const SmallTitle = ({title}: {title: string}) => {
  return (
    <div className='text-base font-semibold text-[#333333] font-hind'>
      {title}
    </div>
  )
}

export default SmallTitle
